---
# kustomization.yaml — terminus-portal k8s raw manifests index
# Applied by ArgoCD Application (infra sync wave, creates namespace).
#
# Sync wave ordering:
#   Wave 1: namespace.yaml — creates terminus-portal namespace

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - external-secret-ghcr.yaml
